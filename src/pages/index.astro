---
import Layout from '../layouts/Layout.astro';

/**
 * V2 de la carta abierta a QlqEstaComida y a la iniciativa QlQ Esta Union.
 * El menu, las promos y el panel siguen en el repo: el home viejo quedo en
 * _index-menu.astro (fuera de las rutas por el guion bajo).
 *
 * El contador de dias cerrados se calcula en cada request (output: 'server'),
 * asi la carta sigue siendo exacta cada dia que pasa sin tener que tocarla.
 */
const TERREMOTO = new Date('2026-06-24T00:00:00-04:00');
// Sin año de fundación: la familia no lo tiene confirmado y prefiere no dar
// una fecha exacta. Esta es la única fórmula que aparece en toda la página.
const TRAYECTORIA = 'más de 46 años';

const ahora = new Date();

/**
 * Todo se cuenta en hora de Caracas. El worker de Cloudflare corre en UTC, y
 * Venezuela va cuatro horas por detrás: sin esto, a partir de las 8 de la
 * noche la página ya daría el día por terminado y contaría uno de más.
 */
const ZONA = 'America/Caracas';
const enCaracas = (fecha: Date) =>
  new Intl.DateTimeFormat('en-CA', { timeZone: ZONA, year: 'numeric', month: '2-digit', day: '2-digit' }).format(fecha);

// Se restan días de calendario en Caracas, no milisegundos entre instantes
const diaDe = (texto: string) => Date.parse(`${texto}T00:00:00Z`);
const diasCerrados = Math.max(0, Math.round((diaDe(enCaracas(ahora)) - diaDe(enCaracas(TERREMOTO))) / 86400000));

/**
 * La fecha de la firma es el día en que se escribió la carta y no cambia:
 * una carta lleva su fecha. Lo que sí sigue contando solo es el número de
 * días cerrados, que es un hecho del local y no de la carta.
 */
const ESCRITA = new Date('2026-09-04T12:00:00-04:00');
const fechaCarta = ESCRITA.toLocaleDateString('es-VE', {
  timeZone: ZONA,
  day: 'numeric',
  month: 'long',
  year: 'numeric',
});

/**
 * Fotos reales, en orden narrativo: antes -> generaciones -> el restaurante
 * -> el cierre -> la esperanza. La seccion no aparece mientras este vacio.
 * Para activarla: poner los archivos en public/fotos/ y descomentar.
 */
const fotos: { src: string; pie: string }[] = [
  // { src: '/fotos/1-antes.webp',        pie: 'El local cuando abrió, en los ochenta.' },
  // { src: '/fotos/2-generaciones.webp', pie: 'Mi abuelo, mi papá y yo.' },
  // { src: '/fotos/3-brasa.webp',        pie: 'El carbón, todos los días a las once de la mañana.' },
  // { src: '/fotos/4-cerrado.webp',      pie: 'El 24 de junio.' },
  // { src: '/fotos/5-esperanza.webp',    pie: 'La cocina, esperando.' },
];

/**
 * El remate se enciende letra a letra. Se parte aquí y no en el navegador
 * para que el HTML llegue ya con las letras: sin JavaScript se ve la frase
 * completa y encendida, no una animación a medias.
 */
const REMATE = 'Solo falta encenderlo.';
let n = 0;
const palabrasRemate = REMATE.split(' ').map((palabra) => ({
  letras: [...palabra].map((letra) => ({ letra, i: n++ })),
}));

const INSTAGRAM = 'https://www.instagram.com/restaurantcabokenedyoficial/';
const MAPS = 'https://maps.app.goo.gl/9rWr5kE7a13ZtMde8?g_st=ic';
const WHATSAPP = `https://wa.me/584142920785?text=${encodeURIComponent('Hola, leí la carta de Cabo Kenedy.')}`;

const title = 'Carta abierta a QlqEstaComida | Cabo Kenedy, La Guaira';
const description = `${TRAYECTORIA[0].toUpperCase() + TRAYECTORIA.slice(1)} en Caraballeda. Llevamos ${diasCerrados} días con el carbón apagado desde el terremoto del 24 de junio. Esta es nuestra carta a QlQ Esta Unión.`;
---

<Layout title={title} description={description}>
  <div class="embers" aria-hidden="true">
    {Array.from({ length: 14 }).map((_, i) => (
      <span class="ember" style={`left:${4 + i * 7}%; animation-delay:${i * 1.7}s; animation-duration:${13 + (i % 5) * 3}s;`} />
    ))}
  </div>

  <main class="relative">
    <!-- ==================== 1. APERTURA ==================== -->
    <header class="escena escena-alta text-center">
      <img
        src="/logo-trim.webp"
        alt="Cabo Kenedy"
        width="484"
        height="369"
        class="w-44 sm:w-52 h-auto mx-auto mb-10"
      />

      <p class="kicker">CARTA ABIERTA A QLQESTACOMIDA</p>

      <h1 class="titular">
        Hace <span class="hero-num">{diasCerrados} días</span><br />
        que no encendemos el carbón.
      </h1>
    </header>

    <!-- ==================== 2. LAS GENERACIONES ==================== -->
    <section class="escena escena-alta text-center" aria-label="Tres generaciones">
      <p class="verso brasa reveal" style="--i:0">A mi papá lo trajo mi abuelo.</p>
      <p class="verso brasa reveal" style="--i:1">A mí me trajo mi papá.</p>
      <p class="verso verso-final brasa reveal" style="--i:2">A mis hijos todavía no he podido traerlos.</p>
    </section>

    <!-- ==================== 3. QUIÉNES SOMOS ==================== -->
    <section class="escena carta" aria-label="Quiénes somos">
      <p class="reveal">
        Buenas, QlqEstaComida. Somos <strong>Cabo Kenedy</strong>, de Caraballeda. Llevamos {TRAYECTORIA} haciendo
        una sola cosa: pollo a la brasa, al carbón, dando vueltas.
      </p>

      <p class="reveal">
        El adobo no está escrito en ningún papel. <span class="destaca">Eso se aprende parado al lado del que lo
        está haciendo.</span> Va con sus arepitas y su ensalada rallada de repollo y zanahoria, esa que la
        gente termina pidiendo aparte porque nunca le alcanza.
      </p>

      <p class="reveal">
        {TRAYECTORIA[0].toUpperCase() + TRAYECTORIA.slice(1)}. Lo mismo, todos los días. A las once
        de la mañana la avenida olía a nosotros y uno sabía qué hora era por el olor.
      </p>
    </section>

    {fotos.length > 0 && (
      <section class="escena" aria-label="Fotos">
        <div class="tira">
          {fotos.map((foto) => (
            <figure class="foto reveal">
              <img src={foto.src} alt={foto.pie} loading="lazy" />
              <figcaption>{foto.pie}</figcaption>
            </figure>
          ))}
        </div>
      </section>
    )}

    <!-- ==================== 4. EL 24 DE JUNIO ==================== -->
    <section class="escena escena-alta text-center" aria-label="El 24 de junio">
      <p class="fecha-grande reveal" aria-hidden="true">24<span>·</span>06</p>
      <p class="verso reveal">Tembló.</p>
      <p class="verso verso-final reveal">No hemos vuelto a abrir.</p>

      <div class="contador reveal">
        <span class="contador-num">{diasCerrados}</span>
        <span class="contador-txt">días cerrados</span>
      </div>
    </section>

    <!-- ==================== 5. LO QUE SE APAGÓ ==================== -->
    <section class="escena carta" aria-label="Lo que se apagó">
      <p class="reveal">
        El señor de los jueves. Las familias que hacían aquí los cumpleaños porque salía más a
        cuenta que alquilar un salón. Los muchachos que llegaban de madrugada con hambre.
      </p>

      <p class="reveal">
        Los que trabajan aquí no están en esa cocina solo por gusto: de ahí comen sus casas.
        Llevan {diasCerrados} días esperando, igual que nosotros.
      </p>
    </section>

    <!-- ==================== 6. LA CHISPA ==================== -->
    <section class="escena carta" aria-label="La chispa">
      <p class="reveal">
        Uno llega a las once y empieza a prender la brasa. Todavía no hay clientes, no
        hay meseros, no hay a quién impresionar.
      </p>

      <p class="reveal">
        Y entonces el carbón empieza a sonar. <strong>Ese chisporroteo tiene ritmo.</strong> Lo
        tiene siempre, pero hay que estar muy callado para oírlo. Uno se queda quieto. El humo sube,
        la grasa cae, la piel empieza a tostarse. Por unos minutos no existe nada más: ni el
        teléfono, ni las cuentas, ni el resto del día.
      </p>

      <p class="linea-brasa reveal">Existe la brasa, el pollo y uno. Nada más.</p>

      <p class="reveal">
        Eso no se aprende en ningún curso. Te agarra en el sitio más común del mundo —una parrilla,
        un mesón, un saco de carbón— y de repente entiendes para qué estás ahí. Cocinar deja de ser
        un oficio y pasa a ser <span class="destaca">la manera que uno tiene de decir algo</span>.
      </p>

      <p class="parrafo-alto reveal">
        Eso es lo que está apagado. No se apagó porque nos cansamos ni porque el pollo dejó de
        gustar. Se apagó porque tembló.
      </p>

      <p class="reveal">
        Pero una chispa se puede volver a encender. Aquí eso se sabe: La Guaira lleva desde el 99
        aprendiendo a rehacerse encima de lo que quedó. Nadie en este pueblo esconde esa cicatriz.
        La señalan.
      </p>

    </section>

    <!-- ==================== 7. KINTSUGI ==================== -->
    <section class="escena kintsugi-section text-center" aria-label="Kintsugi">
      <p class="linea-brasa puente reveal">Del otro lado del mundo le pusieron nombre a esa idea.</p>

      <div class="kintsugi-wrap reveal">
        <svg class="kintsugi-svg" viewBox="0 0 220 220" fill="none" aria-hidden="true">
          <defs>
            <linearGradient id="oro" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stop-color="#bf953f" />
              <stop offset="25%" stop-color="#fcf6ba" />
              <stop offset="50%" stop-color="#b38728" />
              <stop offset="75%" stop-color="#fbf5b7" />
              <stop offset="100%" stop-color="#aa771c" />
            </linearGradient>
            <radialGradient id="halo" cx="50%" cy="50%" r="50%">
              <stop offset="0%" stop-color="rgba(240,176,0,0.10)" />
              <stop offset="100%" stop-color="rgba(240,176,0,0)" />
            </radialGradient>
          </defs>

          <circle cx="110" cy="110" r="105" fill="url(#halo)" class="k-aura" />
          <circle cx="110" cy="110" r="88" class="k-plato" />
          <circle cx="110" cy="110" r="62" class="k-plato k-plato-hondo" />

          <!-- Todas las grietas se quedan dentro del plato: ningún punto pasa
               de un radio de 82, así ni el trazo ni el halo se salen del borde -->
          <path class="k-grieta" style="--d:0.0s" d="M110 30 C 106 56, 118 72, 112 94 C 108 110, 116 126, 110 148 C 106 166, 113 180, 110 190" />
          <path class="k-grieta" style="--d:0.5s" d="M112 94 C 94 90, 78 80, 58 66" />
          <path class="k-grieta" style="--d:0.9s" d="M110 148 C 130 146, 148 152, 168 162" />
          <path class="k-grieta" style="--d:1.2s" d="M112 94 C 128 86, 144 76, 158 60" />
          <path class="k-grieta" style="--d:1.5s" d="M110 148 C 92 156, 78 166, 66 176" />
          <path class="k-grieta k-fina" style="--d:1.8s" d="M58 66 C 50 76, 44 82, 38 88" />
          <path class="k-grieta k-fina" style="--d:2.0s" d="M168 162 C 166 170, 160 176, 152 182" />

          <circle cx="112" cy="94" r="2.6" class="k-nodo" style="--d:2.2s" />
          <circle cx="110" cy="148" r="2.6" class="k-nodo" style="--d:2.35s" />
        </svg>
      </div>

      <p class="k-kanji reveal">金継ぎ</p>
      <h2 class="k-titulo reveal">Kintsugi</h2>
      <p class="k-def reveal">El arte japonés de reparar con oro</p>

      <div class="carta max-w-[34rem] mx-auto mt-6">
        <p class="reveal">
          Los japoneses, cuando se les rompe algo, no lo esconden: rellenan la grieta con oro y la
          dejan a la vista, porque esa grieta también es la historia de la pieza.
        </p>
        <p class="reveal">
          Nosotros no queremos volver a ser exactamente lo que éramos el 23 de junio. Los pedazos
          están todos. El oro también: es la gente que trabaja aquí, la que va a poner el
          pecho el día que esto abra otra vez.
        </p>
      </div>

      <p class="k-remate reveal">El oro ya lo tenemos.</p>
    </section>

    <!-- ==================== 8. LA PETICIÓN ==================== -->
    <section class="escena carta" aria-label="Lo que les pedimos">
      <p class="reveal">
        Así que lo que les pedimos es esto, sin adornos: que se sepa que <strong>Cabo Kenedy sigue
        aquí</strong>.
      </p>

      <p class="reveal">
        Todavía no los podemos sentar a la mesa, porque seguimos cerrados. Cuando volvamos va a
        ser poco a poco: primero solo delivery, hasta que el local aguante otra vez la gente. Pero
        el día que la brasa vuelva a prender, <span class="destaca">el primer pollo sale para
        ustedes</span>. Vengan a Caraballeda y prueben. Si les parece que esto vale la pena,
        cuéntenlo.
      </p>

      <p class="parrafo-alto reveal">
        No estamos pidiendo plata. Estamos pidiendo que alguien cuente que este sitio existe y
        quiere volver a abrir.
      </p>
    </section>

    <!-- ==================== 9. CIERRE ==================== -->
    <section class="escena escena-alta text-center" aria-label="Cierre">
      <p class="verso reveal">El carbón está ahí.</p>
      <p class="verso reveal">La receta está ahí.</p>
      <p class="verso reveal">Nosotros estamos aquí.</p>
      <p class="cierre-remate reveal" aria-label={REMATE}>
        {palabrasRemate.map((palabra, p) => (
          <>
            <span class="palabra" aria-hidden="true">
              {palabra.letras.map(({ letra, i }) => (
                <span class="letra" style={`--i:${i}`}>{letra}</span>
              ))}
            </span>
            {p < palabrasRemate.length - 1 && <span aria-hidden="true"> </span>}
          </>
        ))}
      </p>

      <div class="acciones reveal">
        <a href={WHATSAPP} target="_blank" rel="noopener" class="btn-wa">
          <svg width="17" height="17" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12.05 0C5.5 0 .16 5.34.16 11.89c0 2.1.55 4.14 1.59 5.95L.06 24l6.3-1.65a11.88 11.88 0 0 0 5.69 1.45c6.55 0 11.89-5.34 11.89-11.89S18.6 0 12.05 0Zm0 21.79h-.01a9.87 9.87 0 0 1-5.03-1.38l-.36-.21-3.74.98 1-3.65-.24-.37a9.86 9.86 0 0 1-1.51-5.27c0-5.45 4.44-9.88 9.89-9.88a9.83 9.83 0 0 1 9.88 9.89c0 5.45-4.43 9.89-9.88 9.89Zm5.42-7.4c-.3-.15-1.76-.87-2.03-.97-.27-.1-.47-.15-.67.15-.2.3-.77.97-.94 1.16-.17.2-.35.22-.64.08-.3-.15-1.26-.47-2.4-1.48-.88-.79-1.48-1.76-1.65-2.06-.17-.3-.02-.46.13-.6.13-.14.3-.35.44-.53.15-.17.2-.3.3-.5.1-.19.05-.37-.02-.51-.08-.15-.67-1.62-.92-2.21-.24-.58-.49-.5-.67-.51h-.57c-.2 0-.52.07-.8.37-.27.3-1.03 1.01-1.03 2.48s1.06 2.87 1.21 3.07c.15.2 2.1 3.2 5.08 4.49.7.3 1.26.48 1.69.62.71.23 1.36.2 1.87.12.57-.09 1.75-.72 2-1.41.25-.7.25-1.29.18-1.41-.08-.13-.28-.2-.58-.35Z"/></svg>
          Ayúdanos a encenderlo
        </a>
        <a href={INSTAGRAM} target="_blank" rel="noopener" class="btn-ig">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><rect x="3" y="3" width="18" height="18" rx="5"/><circle cx="12" cy="12" r="3.8"/><circle cx="17.4" cy="6.6" r="1.1" fill="currentColor" stroke="none"/></svg>
          @restaurantcabokenedyoficial
        </a>
      </div>

      <div class="firma reveal">
        <p class="firma-nombre">La familia de Cabo Kenedy</p>
        <p class="firma-lugar">Caraballeda, La Guaira · {TRAYECTORIA} · {fechaCarta}</p>
      </div>
    </section>

    <!-- ==================== PIE ==================== -->
    <footer class="pie">
      <a href={MAPS} target="_blank" rel="noopener" class="pie-dir">
        Local 01, Av. Principal de Caribe, C.C. Houston PB<br />
        Caraballeda 1165, La Guaira
      </a>

      <div class="pie-enlaces">
        <a href={WHATSAPP} target="_blank" rel="noopener">0414-292-0785</a>
        <span aria-hidden="true">·</span>
        <a href={INSTAGRAM} target="_blank" rel="noopener">Instagram</a>
      </div>
    </footer>
  </main>
</Layout>

<style>
  /* ---------- Brasas ---------- */
  .embers {
    position: fixed;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .ember {
    position: absolute;
    bottom: -10px;
    width: 3px;
    height: 3px;
    border-radius: 50%;
    background: var(--color-primary-light);
    box-shadow: 0 0 8px 2px rgba(232, 122, 30, 0.5);
    opacity: 0;
    animation-name: emberRise;
    animation-timing-function: ease-in;
    animation-iteration-count: infinite;
  }

  @keyframes emberRise {
    0%   { transform: translateY(0) translateX(0) scale(1); opacity: 0; }
    12%  { opacity: 0.7; }
    70%  { opacity: 0.35; }
    100% { transform: translateY(-100vh) translateX(28px) scale(0.3); opacity: 0; }
  }

  /* ---------- Escala de espacio ----------
     Una sola serie para todo el documento. Antes cada bloque tenia su
     numero suelto y el salto entre el ritmo interno (14px) y el de las
     escenas (144px) era de 1 a 10: por eso los huecos chocaban. */
  .escena {
    --e2: 0.9rem;
    --e3: 1.5rem;
    --e4: 2.25rem;
    --e5: clamp(1.9rem, 5.5vw, 3rem);

    position: relative;
    max-width: 34rem;
    margin: 0 auto;
    padding: calc(var(--e5) / 2) 1.5rem;
  }

  /* Solo la portada respira de más: es la primera pantalla */
  .escena-alta {
    padding-top: var(--e5);
    padding-bottom: var(--e5);
  }

  @media (min-width: 640px) {
    .escena { padding-left: 2rem; padding-right: 2rem; }
  }

  .kicker {
    font-family: var(--font-display);
    font-size: 0.625rem;
    letter-spacing: 0.32em;
    color: rgba(232, 122, 30, 0.8);
    margin-bottom: 2rem;
  }

  .titular {
    font-family: var(--font-heading);
    font-size: clamp(1.9rem, 7.5vw, 3rem);
    font-weight: 700;
    line-height: 1.18;
    color: var(--color-text-primary);
    text-wrap: balance;
  }

  /* El numero llega encendido y se apaga en cuanto el lector baja: la pagina
     apaga la brasa delante de el. Al final, la ultima linea la vuelve a
     encender. */
  .hero-num {
    color: var(--color-primary);
    text-shadow: 0 0 26px rgba(232, 122, 30, 0.45), 0 0 62px rgba(232, 122, 30, 0.18);
  }

  .hero-num.apagado {
    animation: apagarse 1.8s ease-out forwards;
  }

  @keyframes apagarse {
    0% {
      color: #E87A1E;
      text-shadow: 0 0 26px rgba(232, 122, 30, 0.45), 0 0 62px rgba(232, 122, 30, 0.18);
    }
    12% {
      color: #FFB347;
      text-shadow: 0 0 32px rgba(255, 179, 71, 0.6), 0 0 70px rgba(232, 122, 30, 0.22);
    }
    32% {
      color: #9c8a7c;
      text-shadow: 0 0 8px rgba(232, 122, 30, 0.18);
    }
    48% {
      color: #D08A3C;
      text-shadow: 0 0 18px rgba(232, 122, 30, 0.35);
    }
    70% {
      color: #8f8177;
      text-shadow: 0 0 5px rgba(232, 122, 30, 0.12);
    }
    100% {
      color: #8A7F74;
      text-shadow: none;
    }
  }

  /* ---------- Versos: una linea por respiracion ---------- */
  .verso {
    font-family: var(--font-heading);
    font-size: clamp(1.25rem, 4.6vw, 1.75rem);
    line-height: 1.5;
    color: var(--color-text-secondary);
    margin-bottom: 1.15rem;
    text-wrap: balance;
  }

  .verso-final {
    color: var(--color-text-primary);
    font-weight: 600;
  }

  /* Las tres lineas laten como brasas: la misma respiracion, pero cada una
     entra en su turno, como cuando el carbon se va prendiendo por partes. */
  .brasa {
    animation: latirBrasa 4.8s infinite;
    animation-delay: calc(var(--i) * 1.5s);
  }

  /* El carbon no late como un corazon: sube rapido cuando le entra aire y
     baja despacio. De ahi que el pico este al 22% y no en la mitad. */
  @keyframes latirBrasa {
    0% {
      color: #A3958A;
      text-shadow: none;
    }
    22% {
      color: #FFC77A;
      text-shadow: 0 0 20px rgba(232, 122, 30, 0.65), 0 0 52px rgba(232, 122, 30, 0.28);
    }
    34% {
      color: #FFD9A8;
      text-shadow: 0 0 30px rgba(255, 179, 71, 0.8), 0 0 70px rgba(232, 122, 30, 0.32);
    }
    62% {
      color: #C7B5A4;
      text-shadow: 0 0 12px rgba(232, 122, 30, 0.28);
    }
    100% {
      color: #A3958A;
      text-shadow: none;
    }
  }

  /* La ultima nunca se apaga del todo: es la que duele */
  .verso-final.brasa {
    animation-name: latirBrasaFinal;
  }

  @keyframes latirBrasaFinal {
    0% {
      color: #C9BCB0;
      text-shadow: 0 0 10px rgba(232, 122, 30, 0.15);
    }
    22% {
      color: #FFDCAE;
      text-shadow: 0 0 26px rgba(232, 122, 30, 0.7), 0 0 60px rgba(232, 122, 30, 0.3);
    }
    34% {
      color: #FFF3E4;
      text-shadow: 0 0 36px rgba(255, 179, 71, 0.85), 0 0 80px rgba(232, 122, 30, 0.35);
    }
    62% {
      color: #E4D7C9;
      text-shadow: 0 0 16px rgba(232, 122, 30, 0.35);
    }
    100% {
      color: #C9BCB0;
      text-shadow: 0 0 10px rgba(232, 122, 30, 0.15);
    }
  }

  /* La ultima linea prende como prende un carbon: chispazo, titubeo, y
     despues la brasa que se queda viva. Arranca apagada y se enciende
     cuando entra en pantalla. */
  .cierre-remate {
    margin-top: 1.75rem;
    font-family: var(--font-heading);
    font-size: clamp(1.6rem, 6vw, 2.4rem);
    font-weight: 700;
    text-wrap: balance;
    line-height: 1.25;
  }

  /* Las palabras no se parten al final de la línea aunque cada letra sea su
     propia caja: el salto solo puede ocurrir en los espacios. */
  .palabra {
    display: inline-block;
    white-space: nowrap;
  }

  .letra {
    color: #FFB347;
    text-shadow: 0 0 20px rgba(232, 122, 30, 0.5);
  }

  /* Apagadas solo si hay JS que las pueda encender */
  :global(body.js-reveal) .letra {
    color: #5f554e;
    text-shadow: none;
  }

  /* Se van prendiendo de izquierda a derecha, como corre una mecha */
  :global(body.js-reveal) .cierre-remate.prender .letra {
    animation:
      prenderLetra 1.2s ease-out both,
      latirLetra 4.5s ease-in-out infinite;
    animation-delay: calc(var(--i) * 0.07s), calc(var(--i) * 0.07s + 1.2s);
  }

  @keyframes prenderLetra {
    0%   { color: #5f554e; text-shadow: none; }
    18%  { color: #FFE0B0; text-shadow: 0 0 26px rgba(255, 214, 160, 0.95), 0 0 60px rgba(232, 122, 30, 0.4); }
    45%  { color: #E8913C; text-shadow: 0 0 12px rgba(232, 122, 30, 0.4); }
    70%  { color: #FFC77A; text-shadow: 0 0 22px rgba(232, 122, 30, 0.6); }
    100% { color: #FFB347; text-shadow: 0 0 20px rgba(232, 122, 30, 0.5), 0 0 48px rgba(232, 122, 30, 0.2); }
  }

  /* Ya encendidas, respiran */
  @keyframes latirLetra {
    0%, 100% { text-shadow: 0 0 20px rgba(232, 122, 30, 0.5), 0 0 48px rgba(232, 122, 30, 0.2); }
    50%      { text-shadow: 0 0 28px rgba(255, 179, 71, 0.65), 0 0 62px rgba(232, 122, 30, 0.26); }
  }

  /* ---------- Parrafos ---------- */
  .carta {
    font-size: 1.0625rem;
    line-height: 1.85;
    color: var(--color-text-secondary);
  }

  .carta :global(p) {
    margin-bottom: 1.6rem;
  }

  .carta :global(p:last-child) {
    margin-bottom: 0;
  }

  .carta :global(strong) {
    color: var(--color-text-primary);
    font-weight: 600;
  }

  /* Frase encendida dentro del parrafo */
  .carta :global(.destaca) {
    color: var(--color-primary-light);
    font-weight: 600;
  }

  /* Parrafo entero en tono alto: sube el brillo sin gritar */
  .parrafo-alto {
    color: var(--color-text-primary);
    font-weight: 500;
  }

  /* La frase que sostiene la escena: sola, mas grande y latiendo */
  .puente {
    margin-top: 0 !important;
    margin-bottom: 2.5rem !important;
  }

  .linea-brasa {
    margin: 1.9rem 0 !important;
    font-family: var(--font-heading);
    font-size: clamp(1.2rem, 4.4vw, 1.6rem);
    font-weight: 600;
    line-height: 1.5;
    text-align: center;
    color: var(--color-text-primary);
    text-wrap: balance;
    animation: latirBrasaFinal 4.8s infinite;
  }

  /* ---------- El 24 de junio ---------- */
  .fecha-grande {
    font-family: var(--font-display);
    font-size: clamp(3rem, 14vw, 5rem);
    font-weight: 800;
    line-height: 1;
    color: transparent;
    -webkit-text-stroke: 1px rgba(232, 122, 30, 0.55);
    margin-bottom: 1.5rem;
    letter-spacing: 0.02em;
  }

  .fecha-grande span {
    -webkit-text-stroke: 0;
    color: rgba(232, 122, 30, 0.55);
    margin: 0 0.15em;
  }

  .contador {
    margin-top: 2.25rem;
    display: inline-flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  /* Del mismo tamaño que la fecha de arriba: la fecha va en contorno (lo que
     pasó) y el contador macizo y encendido (lo que cuesta). Con tamaños
     distintos el segundo parecía una nota al pie del primero. */
  .contador-num {
    font-family: var(--font-display);
    font-size: clamp(4rem, 24vw, 7rem);
    font-weight: 800;
    color: var(--color-primary);
    line-height: 1;
    text-shadow: 0 0 30px rgba(232, 122, 30, 0.35);
  }

  .contador-txt {
    font-size: 0.8125rem;
    letter-spacing: 0.24em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  /* ---------- Fotos ---------- */
  .tira {
    display: flex;
    flex-direction: column;
    gap: 3.5rem;
  }

  .foto img {
    width: 100%;
    height: auto;
    border-radius: 1rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    display: block;
  }

  .foto figcaption {
    margin-top: 0.85rem;
    font-size: 0.875rem;
    color: var(--color-text-muted);
    text-align: center;
  }

  /* ---------- Kintsugi ---------- */
  .kintsugi-section {
    max-width: 40rem;
    background: radial-gradient(ellipse 55% 45% at 50% 45%, rgba(240, 176, 0, 0.05), transparent 70%);
  }

  .kintsugi-wrap {
    display: flex;
    justify-content: center;
    margin-bottom: 1.4rem;
  }

  .kintsugi-svg {
    width: clamp(170px, 40vw, 240px);
    height: auto;
    overflow: visible;
  }

  .k-plato { fill: none; stroke: rgba(240, 176, 0, 0.18); stroke-width: 1; }
  .k-plato-hondo { stroke: rgba(240, 176, 0, 0.10); }

  .k-grieta {
    fill: none;
    stroke: url(#oro);
    stroke-width: 2.4;
    stroke-linecap: round;
    stroke-dasharray: 400;
    stroke-dashoffset: 400;
    filter: drop-shadow(0 0 4px rgba(240, 176, 0, 0.55));
  }

  .k-fina { stroke-width: 1.4; }

  .k-nodo {
    fill: url(#oro);
    opacity: 0;
    filter: drop-shadow(0 0 5px rgba(240, 176, 0, 0.8));
  }

  :global(.kintsugi-wrap.visible) .k-grieta {
    animation: dibujarGrieta 1.8s ease-out forwards;
    animation-delay: var(--d, 0s);
  }

  :global(.kintsugi-wrap.visible) .k-nodo {
    animation: aparecerNodo 0.8s ease-out forwards;
    animation-delay: var(--d, 0s);
  }

  @keyframes dibujarGrieta { to { stroke-dashoffset: 0; } }
  @keyframes aparecerNodo { to { opacity: 0.9; } }

  .k-aura { animation: auraPulso 6s ease-in-out infinite; transform-origin: center; }

  @keyframes auraPulso {
    0%, 100% { opacity: 0.5; transform: scale(1); }
    50%      { opacity: 1; transform: scale(1.05); }
  }

  .k-kanji {
    font-size: 0.75rem;
    letter-spacing: 0.4em;
    color: rgba(240, 176, 0, 0.8);
    margin-bottom: 0.9rem;
  }

  .k-titulo {
    font-family: var(--font-heading);
    font-size: clamp(1.6rem, 5vw, 2.25rem);
    font-weight: 700;
    background: linear-gradient(135deg, #bf953f, #fcf6ba, #b38728, #fbf5b7);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    margin-bottom: 0.4rem;
  }

  .k-def {
    font-size: 0.9375rem;
    color: var(--color-text-muted);
  }

  .k-remate {
    margin-top: 1.75rem;
    font-family: var(--font-heading);
    font-size: clamp(1.35rem, 4.5vw, 1.85rem);
    font-weight: 700;
    line-height: 1.4;
    background: linear-gradient(135deg, #e8c66a, #fcf6ba, #f0d878, #fbf5b7);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    text-wrap: balance;
  }

  /* ---------- Acciones ---------- */
  .acciones {
    margin-top: 2rem;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.1rem;
  }

  /* Boton unico y del ancho de su texto: ocupando toda la linea se veia
     pesado justo donde la pagina tiene que sentirse ligera. */
  .btn-wa {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.7rem 1.5rem;
    border-radius: 999px;
    background: var(--color-whatsapp);
    color: #05300f;
    font-family: var(--font-heading);
    font-size: 0.9375rem;
    font-weight: 600;
    transition: transform 0.2s ease, background 0.2s ease;
  }

  .btn-wa:hover { background: #34e07a; }
  .btn-wa:active { transform: scale(0.98); }

  /* Instagram: mismo alto y misma forma que el de WhatsApp, pero hueco, para
     que se lea como la opcion secundaria sin quedar suelto en la pagina. */
  .btn-ig {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.7rem 1.4rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.035);
    border: 1px solid rgba(255, 255, 255, 0.13);
    color: var(--color-text-secondary);
    font-family: var(--font-heading);
    font-size: 0.875rem;
    font-weight: 500;
    transition: transform 0.2s ease, background 0.2s ease, border-color 0.2s ease, color 0.2s ease;
  }

  .btn-ig:hover {
    background: rgba(232, 122, 30, 0.08);
    border-color: rgba(232, 122, 30, 0.4);
    color: var(--color-text-primary);
  }

  .btn-ig:active { transform: scale(0.98); }

  .btn-ig svg { opacity: 0.7; flex-shrink: 0; }

  /* ---------- Firma ---------- */
  .firma {
    margin-top: 2.5rem;
    padding-top: 1.75rem;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .firma-nombre {
    font-family: var(--font-heading);
    font-size: 1.125rem;
    font-weight: 600;
    color: var(--color-text-primary);
  }

  .firma-lugar {
    margin-top: 0.4rem;
    font-size: 0.8125rem;
    color: var(--color-text-muted);
  }

  /* ---------- Pie ---------- */
  .pie {
    max-width: 34rem;
    margin: 0 auto;
    padding: 2rem 1.5rem 3rem;
    text-align: center;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
  }

  .pie-dir {
    display: block;
    padding: 0.5rem 0;
    font-size: 0.875rem;
    line-height: 1.7;
    color: var(--color-text-muted);
  }

  .pie-dir:hover { color: var(--color-text-secondary); }

  .pie-enlaces {
    margin-top: 0.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: var(--color-text-muted);
  }

  /* 44px de alto minimo: con 21px el dedo falla el toque y el enlace parece
     roto aunque el destino sea correcto. */
  .pie-enlaces a {
    display: inline-flex;
    align-items: center;
    min-height: 44px;
    padding: 0 0.5rem;
    color: var(--color-primary);
  }

  .pie-enlaces a:hover { color: var(--color-primary-light); }

  /* ---------- Aparicion al hacer scroll ---------- */
  :global(body.js-reveal) .reveal {
    opacity: 0;
    transform: translateY(18px);
  }

  :global(body.js-reveal) .reveal.visible {
    opacity: 1;
    transform: none;
    transition: opacity 0.5s ease, transform 0.5s ease;
  }

  @media (prefers-reduced-motion: reduce) {
    .ember { display: none; }
    .k-aura { animation: none; }
    .brasa,
    .linea-brasa { animation: none; }
    .hero-num.apagado {
      animation: none;
      color: #8A7F74;
      text-shadow: none;
    }
    .k-grieta { stroke-dashoffset: 0; }
    .k-nodo { opacity: 0.9; }
    :global(body.js-reveal) .letra,
    :global(body.js-reveal) .cierre-remate.prender .letra {
      animation: none;
      color: #FFB347;
      text-shadow: 0 0 20px rgba(232, 122, 30, 0.5);
    }
    :global(body.js-reveal) .reveal,
    :global(body.js-reveal) .reveal.visible {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }
</style>

<script>
  // La brasa del titular se apaga al primer scroll y ya no vuelve.
  const heroNum = document.querySelector('.hero-num');
  if (heroNum) {
    const apagar = () => {
      if (window.scrollY <= 8) return;
      heroNum.classList.add('apagado');
      window.removeEventListener('scroll', apagar);
    };
    window.addEventListener('scroll', apagar, { passive: true });
    // Por si se entra con la página ya bajada (scroll restaurado o un enlace
    // con ancla): en ese caso el momento ya pasó y aparece apagada.
    apagar();
  }

  // El remate se enciende con su propio observador, no con el de aparecer.
  // El otro se adelanta a propósito (para que no se crucen huecos en negro),
  // y con eso la frase prendía mientras todavía estaba fuera de pantalla: si
  // leías la carta entera, al llegar ya estaba encendida. Este exige que la
  // frase esté dentro y bien vista antes de correr la mecha.
  const remate = document.querySelector('.cierre-remate');
  if (remate && 'IntersectionObserver' in window) {
    const ioRemate = new IntersectionObserver((entries) => {
      for (const entry of entries) {
        if (entry.isIntersecting) {
          entry.target.classList.add('prender');
          ioRemate.unobserve(entry.target);
        }
      }
    }, { rootMargin: '0px 0px -22% 0px', threshold: 0.9 });
    ioRemate.observe(remate);
  }

  const reveals = document.querySelectorAll('.reveal');
  if (reveals.length && 'IntersectionObserver' in window) {
    document.body.classList.add('js-reveal');
    const io = new IntersectionObserver((entries) => {
      for (const entry of entries) {
        // Tambien se muestra lo que ya quedo por encima de la pantalla: si
        // alguien entra con el scroll a mitad de pagina, esos bloques nunca
        // llegarian a "entrar" y se quedarian invisibles para siempre.
        const yaPaso = entry.boundingClientRect.top < 0;
        if (entry.isIntersecting || yaPaso) {
          entry.target.classList.add('visible');
          io.unobserve(entry.target);
        }
      }
      // El margen positivo abajo hace que cada bloque aparezca ANTES de
      // entrar en pantalla: si no, se cruza el hueco entre escenas mirando
      // una pantalla negra y el vacio se siente el doble de largo.
    }, { rootMargin: '0px 0px 25% 0px' });
    reveals.forEach((el) => io.observe(el));
  }
</script>
